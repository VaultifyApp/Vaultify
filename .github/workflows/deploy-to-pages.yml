name: Deploy Frontend

on:
    push:
        branches: [main]

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: deploy
              run: |
                  npm install
                  npm deploy
              working-directory: ./frontend
